query PrescriptionTest($id: ID!) {
  prescriptionTest(id: $id) {
    id
    card {
      id
      name
      age
      gender
    }
    result
    paid
    inrolled
    price
    completed
    new
    rx
    created_at
    updated_at
  }
}
