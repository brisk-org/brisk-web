query QuickPrescriptionTests($skip: Float!, $take: Float!) {
  quickPrescriptionTests(skip: $skip, take: $take) {
    id
    name
    price
    paid
    completed
    new
    result
    other
    created_at
    updated_at
  }
}
