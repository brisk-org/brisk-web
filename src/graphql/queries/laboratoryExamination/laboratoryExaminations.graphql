query LaboratoryExaminations($skip: Float!, $take: Float!) {
  laboratoryExaminations(skip: $skip, take: $take) {
    id
    paid
    price
    new
    completed
    card {
      id
      name
    }
    created_at
    updated_at
  }
}
