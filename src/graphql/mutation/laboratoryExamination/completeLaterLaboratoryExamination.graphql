mutation SaveLaboratoryExamination($result: String!, $id: ID!) {
  saveLaboratoryExamination(result: $result, id: $id) {
    id
    paid
    completed
    new
    price
    card {
      name
      phone
    }
    created_at
  }
}
