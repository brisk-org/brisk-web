mutation Register(
  $username: String!
  $password: String!
  $occupation: String!
) {
  register(username: $username, password: $password, occupation: $occupation) {
    errors {
      field
      message
    }
    user {
      id
      username
      occupation
    }
    token
  }
}
