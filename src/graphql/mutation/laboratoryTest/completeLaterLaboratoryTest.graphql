mutation CompleteLaterLaboratoryTest(
  $result: [LaboratoryTestResult!]!
  $id: ID!
) {
  completeLaterLaboratoryTest(result: $result, id: $id) {
    id
    result
    paid
    completed
    new
    price
    card {
      name
      phone
    }
    created_at
  }
}
