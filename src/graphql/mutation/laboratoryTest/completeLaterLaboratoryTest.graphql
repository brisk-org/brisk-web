mutation SaveLaboratoryTest($result: String!, $id: ID!) {
  saveLaboratoryTest(result: $result, id: $id) {
    id
    result
    paid
    completed
    new
    price
    card {
      name
      phone
    }
    created_at
  }
}
