mutation CreatePrescriptionTest(
  $cardId: ID!
  $result: [PrescriptionInput!]!
  $price: Float!
  $rx: String!
) {
  createPrescriptionTest(
    main: { cardId: $cardId, result: $result, price: $price }
    rx: $rx
  ) {
    id
    card {
      name
    }
    price
    result
    rx
  }
}
