mutation MarkPrescriptionTestAsCompleted(
  $id: ID!
  $result: [PrescriptionInput!]!
  $done: Boolean!
) {
  markPrescriptionTestAsCompleted(
    main: { id: $id, result: $result, done: $done }
  ) {
    id
    new
    started
  }
}
